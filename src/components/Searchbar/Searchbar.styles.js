import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textField: {
    marginBottom: 16,
    paddingBottom: 8,
    borderBottomWidth: 1,
    borderBottomColor: 'black',
    fontSize: 24,
  },
});
